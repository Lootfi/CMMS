package project;
import java.util.*;

public class Ordonnanceur {
	
	public FiledAttente ordon(FiledAttente F) {
		Iterator<Processus> iter = F.iterator();
		do {
		}while(iter.hasNext());
		return F;
	}
	
}
